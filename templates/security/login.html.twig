{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}

<div class="auth-container">
    <div class="auth-title">
        <img src="{{ asset('/images/logo.png') }}" alt="Logo TaskLinker" />
    </div>
    <div class="auth-content px-20 py-32">
        <form method="post">
            {% if error %}
                <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}

            {% if app.user %}
                <div class="mb-3">
                    You are logged in as {{ app.user.userIdentifier }}, <a href="{{ logout_path() }}">Logout</a>
                </div>
            {% endif %}

            <h1 class="text-white text-[34px] font-bold text-center mb-10">Se connecter</h1>
            <label for="inputEmail" class="text-white text-[16px]">Email</label>
            <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
            <label for="inputPassword" class="text-white text-[16px]">Mot de passe</label>
            <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>
            <input type="hidden" name="_csrf_token" data-controller="csrf-protection" value="{{ csrf_token('authenticate') }}">

            <button class="mt-8 w-full py-[25px] text-white bg-[#1C5B50] text-[14px] rounded-xl" type="submit">
                Continuer
            </button>
            <p class="text-white text-center mt-4 text-[14px]">
                Pas de compte ?
                <a href="{{ path('app_register') }}" class="text-white underline">
                    S'inscrire
                </a>
            </p>
        </form>
    </div>
</div>

{% endblock %}
